<Window
    x:Class="AudioRouterWindows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Title="AudioRouter">

    <Grid Background="#0B0D0F" Padding="24" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Width="56" Height="56" CornerRadius="14" BorderBrush="#66D8D0A0" BorderThickness="2" Background="#10211F">
                <TextBlock Text="AU" Foreground="#8FE9DE" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="AudioRouter" Foreground="#F5F5F3" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="Windows routing console" Foreground="#A0A0A0" FontSize="14" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="RefreshButton" Click="RefreshButton_Click" Content="Refresh" />
                <Button x:Name="SoundSettingsButton" Click="SoundSettingsButton_Click" Content="Sound Settings" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" CornerRadius="14" Background="#121619" BorderBrush="#252B30" BorderThickness="1" Padding="16">
            <StackPanel Spacing="8">
                <TextBlock x:Name="StatusText" Text="Loading Windows Core Audio..." Foreground="#F5F5F3" FontSize="16" FontWeight="SemiBold" />
                <TextBlock x:Name="CapabilityText" Text="Endpoint and session controls are live. True per-app output routing is experimental until the process-loopback renderer is complete." Foreground="#A0A0A0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" ColumnSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border CornerRadius="16" Background="#101416" BorderBrush="#293035" BorderThickness="1">
                <Grid RowSpacing="12" Padding="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Routes" Foreground="#F5F5F3" FontSize="22" FontWeight="SemiBold" />
                            <TextBlock Text="Select a Windows audio session, then adjust volume or assign a target." Foreground="#A0A0A0" />
                        </StackPanel>
                        <TextBlock x:Name="SourceCountText" Grid.Column="1" Foreground="#E6B35A" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </Grid>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="SourceList" Spacing="10" />
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="1" CornerRadius="16" Background="#101416" BorderBrush="#293035" BorderThickness="1">
                <Grid RowSpacing="12" Padding="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Outputs" Foreground="#F5F5F3" FontSize="22" FontWeight="SemiBold" />
                            <TextBlock Text="Windows endpoints with live volume and mute where supported." Foreground="#A0A0A0" />
                        </StackPanel>
                        <TextBlock x:Name="OutputCountText" Grid.Column="1" Foreground="#8FE9DE" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </Grid>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="OutputList" Spacing="10" />
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
